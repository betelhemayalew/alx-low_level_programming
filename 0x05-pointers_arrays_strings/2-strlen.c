#include "main.h"
#include <stdio.h>

/**
 *_strlen - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;
    int len;

    str = "My first strlen!";
    len = _strlen(str);
   int _strlen(char *s); ("%d\n", len);
    return (0);
}
